{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -R logo-small.png assets && cp -R logo.png assets",
    "apps:server": "vite build && cp -R logo-small.png assets && cp -R logo.png assets && zat serve",
    "watch": "vite build --watch",
    "preview": "vite preview"
  },
  "dependencies": {
    "@zendeskgarden/tailwindcss": "^3.0.1",
    "vue": "^3.2.41"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.2.0",
    "autoprefixer": "^10.4.13",
    "postcss": "^8.4.18",
    "tailwindcss": "^3.2.1",
    "vite": "^3.2.0"
  }
}
